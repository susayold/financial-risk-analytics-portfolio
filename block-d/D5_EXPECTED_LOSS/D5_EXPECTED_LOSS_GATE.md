# D5 Expected Loss Gate

Status: `PASS_WITH_LIMITATIONS`. The engine is `EL_MAIN_ANALYTICAL = p_bad_final × lgd_proxy × ead_proxy`. Outputs are an analytical expected-loss proxy only, not IFRS 9, Basel, realized loss, or production ECL.
